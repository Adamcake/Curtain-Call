if(spin) {
	x = xstart + lengthdir_x(point_distance(xstart, ystart, x, y), point_direction(xstart, ystart, x, y) + 2.14 * d);
	y = ystart + lengthdir_y(point_distance(xstart, ystart, x, y), point_direction(xstart, ystart, x, y) + 2.14 * d);
}
